{
  "name": "snapcloud",
  "version": "0.1.0",
  "description": "From need to AWS blueprint in 30 seconds",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node-esm src/index.ts",
    "worker": "ts-node-esm src/workflow/worker.ts",
    "snap:it": "node scripts/snap-it.js",
    "snap:stub": "node scripts/snap-stub.js",
    "snap:reset": "node scripts/snap-reset.js"
  },
  "dependencies": {
    "@temporalio/client": "^1.12.1",
    "@temporalio/common": "^1.12.1",
    "@temporalio/worker": "^1.12.1",
    "@temporalio/workflow": "^1.12.1",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.848.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.5"
  }
}
